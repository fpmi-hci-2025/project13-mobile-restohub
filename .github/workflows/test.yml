name: Simple Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: List files (debug)
      run: |
        echo "=== Корневая структура ==="
        ls -la
        echo "=== Содержимое RestoHub ==="
        ls -la RestoHub/
    
    - name: Build project
      run: |
        cd RestoHub
        xcodebuild build \
          -project RestoHub.xcodeproj \
          -scheme RestoHub \
          -destination 'platform=iOS Simulator,name=iPhone 15'
    
    - name: Run tests
      run: |
        cd RestoHub
        xcodebuild test \
          -project RestoHub.xcodeproj \
          -scheme RestoHub \
          -destination 'platform=iOS Simulator,name=iPhone 15'
